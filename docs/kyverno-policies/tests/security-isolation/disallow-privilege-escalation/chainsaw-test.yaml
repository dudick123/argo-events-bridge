apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: disallow-privilege-escalation
spec:
  steps:
    - name: install-policy-set-enforce
      try:
        - apply:
            file: ../../../policies/security-isolation/policies.yaml
        - patch:
            resource:
              apiVersion: kyverno.io/v1
              kind: ClusterPolicy
              metadata:
                name: disallow-privilege-escalation
              spec:
                validationFailureAction: Enforce
        - assert:
            resource:
              apiVersion: kyverno.io/v1
              kind: ClusterPolicy
              metadata:
                name: disallow-privilege-escalation
              status:
                conditions:
                  - reason: Succeeded
                    status: "True"
                    type: Ready
    - name: verify-policy-behavior
      try:
        - apply:
            file: good-pod.yaml
        - apply:
            file: bad-pod.yaml
            expect:
              - check:
                  ($error != null): true
