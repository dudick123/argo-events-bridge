apiVersion: batch/v1
kind: Job
metadata:
  name: bad-job
  labels:
    platform.example.com/tenant: sample-tenant
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: job
          image: platformacr.azurecr.io/migrate:1.0.0
